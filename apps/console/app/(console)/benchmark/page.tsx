import { Benchmark } from "@/components/benchmark/benchmark";

/** SCR-601 Benchmark — the detection benchmark run and its gate report (GMCP-61). */
export default function BenchmarkPage() {
  return <Benchmark />;
}
