name: default
version: 1.0.0
description: Intentionally broken default pack fixture (requirement #4)
dsl_version: 1
default_action: allow
evaluation_strategy: severity-max
policies:
  - policies/broken.yaml
