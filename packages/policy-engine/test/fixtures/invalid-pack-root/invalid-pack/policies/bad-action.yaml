# Intentionally invalid: action value is not in the allowed enum.
id: block_bad_action_example
pack: invalid-pack
priority: 100
match:
  direction: request
  tool: read_file
  args:
    path_regex: '(^|/)\.env$'
action: blck
severity: critical
message: This fixture exists to test enum validation.
