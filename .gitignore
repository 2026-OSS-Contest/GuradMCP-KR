node_modules/
.next/
dist/
build/
# Eclipse/VS Code Java output. Gradle builds into build/ above; bin/ appears only
# because an IDE opened the project, and its contents duplicate src/.
bin/
.gradle/
bin/
coverage/
playwright-report/
test-results/
artifacts/
reports/*.json
reports/*.html
.env
*.log
.DS_Store
.idea/
*.tsbuildinfo

.omc

# Playwright MCP scratch (created at repo root during dev)
.playwright-mcp/

# Synthetic demo fixture — not a real secret, deliberately tracked (GMCP-19).
!apps/demo-mcp-tools/sandbox/.env